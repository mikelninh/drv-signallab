[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "drv-signallab"
version = "0.1.0"
description = "Trustworthy data and AI monitoring over synthetic public-administration data"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["signallab*"]
